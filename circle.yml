machine:
  timezone: America/New_York
  node:
   version: 6.11.0

dependencies:
  override:
    - npm install

test:
  override:
    - npm test

deployment:
  development:
    branch: circleb
    commands:
      - npm run build
      - aws s3 sync /home/ubuntu/profilepage/build s3://staging-challenge/ --delete  --acl public-read
